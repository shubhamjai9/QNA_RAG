---
version: 1
runtime: python311
build:
  commands:
    build:        
      - pip3 install pipenv
      - pipenv install
run:
  runtime-version: 3.11
  pre-run:
    - pip3 install -r requirements.txt
    - pip3 list
  command: python3 app.py
  network:
    port: 5000
